<!-- Copyright © 2025 Timothy Ellis, Fyrby Additive Manufacturing & Engineering -->

<Window x:Class="ProgrammerForFeetechServos.Views.ChangeIdDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Change Servo ID" 
        Height="650" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#1976D2"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#212121"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>

        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#9E9E9E"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel Margin="32">
            <!-- Header -->
            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,24">
                <TextBlock FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="⚙"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChanging}" Value="False">
                                    <Setter Property="Text" Value="⚠"/>
                                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsChanging}" Value="True">
                                    <Setter Property="Text" Value="⚙"/>
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="Change Servo ID" FontSize="22" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>

                <TextBlock FontSize="16" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,8,0,0">
                    <Run Text="Current ID: "/><Run Text="{Binding CurrentId, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>

            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" 
                    Padding="0,24,0,0" Margin="0,0,0,20"/>

            <!-- Progress view (shown when changing) -->
            <StackPanel Visibility="{Binding IsChanging, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,0,0,20">
                <Border Background="{StaticResource SurfaceBrush}" 
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="8" Padding="20">
                    <StackPanel>
                        <ProgressBar IsIndeterminate="True" Height="4" BorderThickness="0" Margin="0,0,0,16"/>
                        <TextBlock Text="{Binding ProgressMessage}" 
                                   FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"
                                   TextWrapping="Wrap" TextAlignment="Center" MinHeight="40"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Input view (shown when not changing) -->
            <StackPanel Visibility="{Binding IsChanging, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}">
                <!-- Input field -->
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="New ID (1-252)" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                    
                    <TextBox Text="{Binding NewIdInput, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="18" Padding="12,8"
                             BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                             MaxLength="3"/>

                    <TextBlock FontSize="11" Foreground="#F44336" Margin="0,6,0,0"
                               Visibility="{Binding IsValid, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}">
                        <Run Text="⚠ Invalid ID or same as current"/>
                    </TextBlock>
                </StackPanel>

                <!-- Warning message -->
                <Border Background="#FFF3E0" BorderBrush="{StaticResource WarningBrush}" 
                        BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="ℹ" FontSize="16" Foreground="{StaticResource PrimaryBrush}" 
                                       Margin="0,0,8,0"/>
                            <TextBlock Text="Important Notes:" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" 
                                       Margin="0,2" TextWrapping="Wrap">
                                <Run Text="• Ensure the new ID is not already in use"/>
                            </TextBlock>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" 
                                       Margin="0,2" TextWrapping="Wrap">
                                <Run Text="• The servo will restart with the new ID"/>
                            </TextBlock>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" 
                                       Margin="0,2" TextWrapping="Wrap">
                                <Run Text="• This change is permanent (stored in EEPROM)"/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Spacer -->
            <Border Height="20"/>

            <!-- Buttons -->
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" 
                    Padding="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource SecondaryButton}" 
                            Command="{Binding CancelCommand}"
                            Content="Cancel" Margin="0,0,12,0"/>
                    
                    <Button Style="{StaticResource DialogButton}" 
                            Command="{Binding ConfirmCommand}"
                            Content="Change ID"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>
