<Window x:Class="ProgrammerForFeetechServos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProgrammerForFeetechServos"
        xmlns:viewmodels="clr-namespace:ProgrammerForFeetechServos.ViewModels"
        mc:Ignorable="d"
        Title="Programmer For Feetech Servos" 
        Height="700" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Modern Color Scheme -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#1976D2"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#F44336"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#212121"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        </Style>

        <!-- ListBox Style -->
        <Style x:Key="ServoListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <!-- ListBox Item Style -->
        <Style x:Key="ServoListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left side - Port selector and connection -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13"/>
                    <ComboBox Style="{StaticResource ModernComboBox}"
                              ItemsSource="{Binding ServoManager.AvailablePorts}"
                              SelectedItem="{Binding ServoManager.SelectedPort}"
                              IsEnabled="{Binding ServoManager.IsScanning, Converter={StaticResource InverseBoolConverter}}"
                              Width="150" Margin="0,0,12,0"/>
                    
                    <Button Style="{StaticResource ModernButton}" 
                            Command="{Binding RefreshPortsCommand}"
                            IsEnabled="{Binding ServoManager.IsScanning, Converter={StaticResource InverseBoolConverter}}"
                            Padding="8" Margin="0,0,16,0" ToolTip="Refresh Ports">
                        <TextBlock Text="??" FontSize="14"/>
                    </Button>

                    <!-- Connection Status -->
                    <Border CornerRadius="12" Padding="12,6" Margin="0,0,12,0"
                            Background="#F0F0F0" MaxWidth="400">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="Gray"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ServoManager.ConnectionState.State}" Value="Connected">
                                                <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ServoManager.ConnectionState.State}" Value="Connecting">
                                                <Setter Property="Fill" Value="{StaticResource WarningBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ServoManager.ConnectionState.State}" Value="Error">
                                                <Setter Property="Fill" Value="{StaticResource ErrorBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding ServoManager.StatusText}" 
                                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       TextWrapping="Wrap"
                                       MaxWidth="360"/>
                        </StackPanel>
                    </Border>

                    <Button Style="{StaticResource ModernButton}" 
                            Command="{Binding ConnectCommand}"
                            IsEnabled="{Binding ServoManager.IsScanning, Converter={StaticResource InverseBoolConverter}}"
                            Content="Connect" Margin="0,0,8,0"
                            Visibility="{Binding ServoManager.IsDisconnected, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <Button Style="{StaticResource ModernButton}" 
                            Command="{Binding DisconnectCommand}"
                            IsEnabled="{Binding ServoManager.IsScanning, Converter={StaticResource InverseBoolConverter}}"
                            Content="Disconnect"
                            Visibility="{Binding ServoManager.IsConnected, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Right side - Help and About -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource ModernButton}" 
                            Command="{Binding ShowHelpCommand}"
                            Content="Help" Margin="0,0,8,0" Padding="12,6"/>
                    <Button Style="{StaticResource ModernButton}" 
                            Command="{Binding ShowAboutCommand}"
                            Content="About" Padding="12,6"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="250"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Servo List -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0" Background="{StaticResource BackgroundBrush}" 
                            Padding="16,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBlock Text="Servos" FontSize="18" FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="{Binding Servos.Count}" FontSize="16" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>

                    <!-- Scan Progress -->
                    <Border Grid.Row="1" Background="{StaticResource BackgroundBrush}" 
                            Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                            Visibility="{Binding ServoManager.IsScanning, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Scanning..." FontSize="11" 
                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock HorizontalAlignment="Right" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}">
                                    <Run Text="{Binding ServoManager.ScanProgress, StringFormat=P0, Mode=OneWay}"/>
                                </TextBlock>
                            </Grid>
                            <ProgressBar Value="{Binding ServoManager.ScanProgress}" 
                                         Maximum="1" Height="4" BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <!-- Servo List -->
                    <ListBox Grid.Row="2" Style="{StaticResource ServoListBox}"
                             ItemsSource="{Binding Servos}"
                             SelectedItem="{Binding SelectedServo}"
                             ItemContainerStyle="{StaticResource ServoListBoxItem}"
                             Padding="8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="??" FontSize="24" 
                                               Margin="0,0,12,0" VerticalAlignment="Center"
                                               Foreground="{StaticResource SuccessBrush}"/>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" FontSize="14"
                                                   Foreground="{StaticResource TextPrimaryBrush}">
                                            <Run Text="ID: "/><Run Text="{Binding Id}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                            <Run Text="Model: "/><Run Text="{Binding ModelNumber}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Empty State -->
                    <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                                Visibility="{Binding Servos.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                        <TextBlock Text="??" FontSize="48" HorizontalAlignment="Center" 
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>
                        <TextBlock Text="No servos found" FontSize="16" FontWeight="SemiBold"
                                   HorizontalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Connect and scan to find servos" FontSize="12"
                                   HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Refresh Button -->
                    <Border Grid.Row="3" Background="{StaticResource BackgroundBrush}" 
                            Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                        <Button Style="{StaticResource ModernButton}" 
                                Command="{Binding ScanServosCommand}"
                                Content="??  Refresh Scan" 
                                HorizontalAlignment="Stretch"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="1" Background="{StaticResource BorderBrush}" 
                          HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

            <!-- Right Panel - Detail View -->
            <Grid Grid.Column="2" Background="{StaticResource SurfaceBrush}">
                <!-- Servo Detail View -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding SelectedServo, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="??" FontSize="80" HorizontalAlignment="Center" 
                               Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,24"/>

                    <StackPanel DataContext="{Binding SelectedServo}">
                        <TextBlock HorizontalAlignment="Center" FontSize="36" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8">
                            <Run Text="Servo ID: "/><Run Text="{Binding Id}"/>
                        </TextBlock>

                        <TextBlock HorizontalAlignment="Center" FontSize="18" 
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,32">
                            <Run Text="Model: "/><Run Text="{Binding ModelNumber}"/>
                        </TextBlock>

                        <Button Style="{StaticResource ModernButton}" 
                                Command="{Binding DataContext.ChangeServoIdCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                IsEnabled="{Binding DataContext.ServoManager.IsScanning, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBoolConverter}}"
                                Content="?  Change Servo ID" 
                                Padding="20,12" FontSize="15" FontWeight="SemiBold"
                                HorizontalAlignment="Center" MinWidth="220"/>
                    </StackPanel>
                </StackPanel>

                <!-- No Selection View -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding SelectedServo, Converter={StaticResource NullToVisibilityConverterInverse}}">
                    <TextBlock Text="??" FontSize="64" HorizontalAlignment="Center" 
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>
                    <TextBlock Text="Select a servo" FontSize="20" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="12,8">
            <TextBlock Text="{Binding ServoManager.StatusMessage}" 
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
        </Border>
    </Grid>
</Window>
